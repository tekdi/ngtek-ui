import { ImageRotationDirective } from './image-rotation.directive';

describe('ImageRotationDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageRotationDirective();
    expect(directive).toBeTruthy();
  });
});
